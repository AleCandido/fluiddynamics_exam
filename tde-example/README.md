# Tidal Destruction Event (TDE) example

The process include:

- star initialization and relaxation, see [`star.bash`](./star.bash)
  - and also test evolution
- put the star on orbit for TDE

To run them all just launch:

```sh
bash test/full.bash
```
